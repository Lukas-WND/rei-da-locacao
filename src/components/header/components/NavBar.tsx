export default function NavBar() {
  return (
    <div>
      <nav className="flex gap-8 items-center h-full text-lg font-secondary">
        <a href="#">Sobre</a>
        <a href="#">Contato</a>
        <a href="#">Produtos</a>
        <a href="#">Localização</a>
      </nav>
    </div>
  );
}
